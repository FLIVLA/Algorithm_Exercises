from array import array

class UF:
    def __init__(self, N):
        self.count = N;
        self.id = array('i', range(N))
    
    def quickFind(p):
        return id[p]
    
    def union(p, q):
        pId = quickFind(p)
        qId = quickFind(q)
        
        if (pId == qId): return
        
        for (i in id.)
    
    def connected(p, q):
        return 